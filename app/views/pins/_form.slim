= simple_form_for @pin, :remote => true, :html => { :'data-type' => 'html', :id => 'create_pin_form' } do |f|
  #url-field = f.input :url
  .validation-error
  #note-field = f.input :note
  #file-field = f.attachment_field :file
  #add-pin = f.submit
.modal-footer
